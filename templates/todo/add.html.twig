{% extends 'base.html.twig' %}

{% block title %}{{ pageTitle }}{% endblock %}

{% block body %}
    <header class="flex items-center justify-center gap-4 py-8">
        {{ ux_icon('tabler:new-section', { class: 'h-24 text-emerald-800' }) }}
        <div class="">
            <h1 class="text-3xl font-semibold">{{ pageTitle ?? '' }}</h1>
            <p class="text-base text-emerald-600">
                {{ pageSubtitle ?? '' }}
            </p>
        </div>
    </header>
    <div class="p-4">
        {{ include('todo/form.html.twig') }}
    </div>
{% endblock %}